package inheritance.iinterface;

public interface Shape {
	// prior to JDK 1.7 version >> interface contains abstract metods
	// Mặc định s>> public abstract
	// abstract method >> no body
	void drawing();
}
