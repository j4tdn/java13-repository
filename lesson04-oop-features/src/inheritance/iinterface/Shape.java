package inheritance.iinterface;

public interface Shape {
	// Prior to JAVA07(JDK 1.7) version >> interface contains abtract methods
	// abstract method >> method with no body
	// Mac dinh >> public abstract
	void drawing();
	
	// From JDK 1.8 version
}
